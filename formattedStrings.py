first = "Paul"
last = "Smith"

# prefix your strings with an F and then use curly braces.

name = f'{first} [{last}] is a coder.'

print(name)

# ==== String Method ====

# len function is built into Python. It's a general purpose function,
# so it's not limited to counting the number of characters in a string.

course = "learning Python to become a developer"
print(len(course))
# print(course.upper())
print(course.find('become'))
print(course.replace('Python', 'Javascript'))
print('Python' in course)


# start again at 48:35~
